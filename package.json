{
  "name": "lenia",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server-rest": "json-server --watch db.json --port 3001",
    "start": "npm run server-rest & gulp"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-plugin-transform-es2015-modules-commonjs": "^6.14.0",
    "babelify": "^7.3.0",
    "gulp-appfy-tasks": "^3.5.1",
    "json-server": "^0.8.21"
  },
  "browserify": {
    "transform": [
      [
        "babelify"
      ]
    ]
  },
  "babel": {
    "plugins": [
      "transform-es2015-modules-commonjs"
    ]
  },
  "dependencies": {
    "bootstrap": "^3.3.7",
    "mithril": "^0.2.5"
  }
}
